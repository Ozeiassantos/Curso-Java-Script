let valores = [8, 5, 5, 9, 2]
valores.sort()
//console.log(valores)



for(let pos in valores) {
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)


    // só funciona para matrizes (areis) e objetos!!

}

// num.indexOf(9)   para buscar no numero no vetor